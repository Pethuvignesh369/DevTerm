feat: final polish - onboarding, host groups, animations, sounds

- Onboarding wizard (6-step tour on first launch, stored in localStorage)
- Host groups tree view component (collapsible folders with host count)
- Staggered list animations (cards fade in sequentially)
- Hover lift effect on host cards (translateY + shadow)
- Shimmer skeleton animation utility
- Pulse ring for active connections
- Notification sounds via Web Audio API (connect/disconnect/error tones)
- Disconnect notification with sound
- Host edit + duplicate buttons on cards
